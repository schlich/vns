[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vns"
version = "0.1.0"
dependencies = [
    "nbdev"
]

[tool.ruff]
extend-include = ["*.ipynb"]
fix = true
select = ["ALL"]
