{
  "name": "VNS Analysis",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bullseye",
  "features": {
    "ghcr.io/mamba-org/devcontainer-features/micromamba:1": {}
  },
  "postCreateCommand": "cp .devcontainer/.zshrc $HOME && cp .devcontainer/.p10k.zsh $HOME && micromamba shell init --shell zsh --root-prefix=~/micromamba &&micromamba create -f environment.yml"
  // see GH repo: Gagandeep39/vscode-devcontainer-ohmyzsh
}
